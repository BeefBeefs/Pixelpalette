(()=>{const search=document.getElementById('systemSearch'),cards=[...document.querySelectorAll('.system-card')],empty=document.getElementById('emptySearch'),count=document.getElementById('visibleSystemCount');function apply(){const q=(search?.value||'').trim().toLowerCase();let visible=0;for(const card of cards){const ok=!q||(card.dataset.search||card.textContent).toLowerCase().includes(q);card.hidden=!ok;if(ok)visible++}if(count)count.textContent=`${visible} shown`;if(empty)empty.style.display=visible?'none':'block'}search?.addEventListener('input',apply);apply();})();